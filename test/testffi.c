#include <stdio.h>

int sumTwo(int a, int b) {
    return a + b;
}

/*
void main() {
    int a, b;
    puts("Enter two numbers, sep by space:");
    scanf("%d %d", &a, &b);
    printf("%d + %d = %d \n", a, b , sumTwo(a, b));
}
*/

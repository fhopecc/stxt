編碼(encoding)
--------------
0990516

unicode
~~~~~~~
0990516

unicode 是一個邏輯編碼，非實體編碼，
它是將自然數對映到字元的表。

例如「品」這個字其 unicode 之編碼為 U+54C1。

unicode 在電腦的實際編碼有 UCS2，以 2 位元組作編碼；
UCS4 以 4 位元組作編碼；
UTF16 大部份字元用 2 位元組作編碼，但少部份用 4 位元組；
UTF8 則用 1 至 6 位元組作編碼。

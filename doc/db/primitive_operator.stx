原始運算子與衍生運算子
----------
原始運算子無法以其他的運算子來定義，
而衍生運算子可由原始運算子上述推演出來。
原始運算子包含：
* UNION
* MINUS
* TIMES
* PROJECT
* RESTRICT

衍生運算子包含：
* INTERSECT
* JOIN
* DIVIDEBY

theorem.INTERSECT 為衍生運算子
R INTERSECT S = R MINUS (R MINUS S)

或

R INTERSECT S = S MINUS (S MINUS R)

theorem.JOIN 為衍生運算子
若 R = (A, B) 且 S = (C, D)
A 及 C 為 JOIN 的共同欄，則

R JOIN S = ((R TIMES S) WHERE R(A) = S(C))[A,B,D]

或

R JOIN S = ((R TIMES S) WHERE R(A) = S(C))[B,C,D]
